import colors from "../../../../Constants/colors";

const styles = {
    pageTitle: {
        fontWeight: 'bold',
        fontSize: 24,
        color: colors.primaryColor
    },

    flex: {
        display: 'flex',
        alignItems: 'center',
    },

    select: {
        borderRadius: 5,
        fontSize: 14,
    }
}

export default styles;